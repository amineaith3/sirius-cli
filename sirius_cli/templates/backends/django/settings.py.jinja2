"""
Django settings for the generated {{ project_name }} backend.

WARNING: This file is auto-generated by Sirius-CLI.
- Set SECRET_KEY via the SECRET_KEY environment variable in production.
- Set DATABASE_URL via the DATABASE_URL environment variable when using PG or MySQL.
"""
import os
from pathlib import Path

BASE_DIR = Path(__file__).resolve().parent

# Security
# WARNING: Keep the secret key used in production secret.
SECRET_KEY = os.environ.get(
    "SECRET_KEY",
    "django-insecure-change-me-in-production-{{ project_name }}",
)

DEBUG = os.environ.get("DEBUG", "True") == "True"

ALLOWED_HOSTS = ["*"]

# Application definition
INSTALLED_APPS = [
    "django.contrib.contenttypes",
    "django.contrib.auth",
    "corsheaders",
    "rest_framework",
{% if auth %}
    "rest_framework_simplejwt",
{% endif %}
    "backend",
]

MIDDLEWARE = [
    "django.middleware.security.SecurityMiddleware",
    "corsheaders.middleware.CorsMiddleware",
    "django.middleware.common.CommonMiddleware",
]

SECURE_BROWSER_XSS_FILTER = True
SECURE_CONTENT_TYPE_NOSNIFF = True
X_FRAME_OPTIONS = "DENY"

ROOT_URLCONF = "backend.urls"

WSGI_APPLICATION = "backend.wsgi.application"

# Database
{% if db_type == 'pg' %}
# PostgreSQL connection
# Set DATABASE_URL in your environment for production.
# Example: DATABASE_URL=postgresql://user:password@host:5432/dbname
DATABASES = {
    "default": {
        "ENGINE": "django.db.backends.postgresql",
        "NAME": os.environ.get("PGDATABASE", "{{ project_name | default('app', true) }}"),
        "USER": os.environ.get("PGUSER", "postgres"),
        "PASSWORD": os.environ.get("PGPASSWORD", ""),
        "HOST": os.environ.get("PGHOST", "localhost"),
        "PORT": os.environ.get("PGPORT", "5432"),
    }
}
{% elif db_type == 'mysql' %}
# MySQL connection
# Set the individual MY* env vars in your environment for production.
DATABASES = {
    "default": {
        "ENGINE": "django.db.backends.mysql",
        "NAME": os.environ.get("MYSQL_DATABASE", "{{ project_name | default('app', true) }}"),
        "USER": os.environ.get("MYSQL_USER", "root"),
        "PASSWORD": os.environ.get("MYSQL_PASSWORD", ""),
        "HOST": os.environ.get("MYSQL_HOST", "localhost"),
        "PORT": os.environ.get("MYSQL_PORT", "3306"),
    }
}
{% else %}
# SQLite (default) — file stored alongside the backend package
DATABASES = {
    "default": {
        "ENGINE": "django.db.backends.sqlite3",
        "NAME": BASE_DIR / "app.db",
    }
}
{% endif %}

DEFAULT_AUTO_FIELD = "django.db.models.BigAutoField"

{% if auth %}
AUTH_USER_MODEL = "backend.AppUser"
{% endif %}

# Django REST Framework
REST_FRAMEWORK = {
{% if auth %}
    "DEFAULT_AUTHENTICATION_CLASSES": [
        "rest_framework_simplejwt.authentication.JWTAuthentication",
    ],
    "DEFAULT_PERMISSION_CLASSES": [
        "rest_framework.permissions.IsAuthenticated",
    ],
{% else %}
    "DEFAULT_AUTHENTICATION_CLASSES": [],
    "DEFAULT_PERMISSION_CLASSES": [
        "rest_framework.permissions.AllowAny",
    ],
{% endif %}
    "DEFAULT_PAGINATION_CLASS": "rest_framework.pagination.CursorPagination",
    "PAGE_SIZE": 100,
}

{% if auth %}
# Simple JWT configuration
from datetime import timedelta

SIMPLE_JWT = {
    "ACCESS_TOKEN_LIFETIME": timedelta(days=7),
    "AUTH_HEADER_TYPES": ("Bearer",),
}
{% endif %}

# CORS — configurable via ALLOWED_ORIGINS env variable or fallback to dev origins
_env_origins = os.environ.get("ALLOWED_ORIGINS")
CORS_ALLOWED_ORIGINS = [o.strip() for o in _env_origins.split(",")] if _env_origins else [
    "http://localhost:5173",
    "http://127.0.0.1:5173",
    "{{ api_url }}",
]
CORS_ALLOW_CREDENTIALS = True

# Internationalisation
LANGUAGE_CODE = "en-us"
TIME_ZONE = "UTC"
USE_I18N = True
USE_TZ = True
